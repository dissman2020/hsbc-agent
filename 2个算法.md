## 1. 基于语义约束规则图的 Policy 缺陷检测算法

这是一个“上线前的静态检查算法”。

它不执行真实业务，而是检查Policy规则之间是否存在逻辑问题。

**输入：**

- 选定的一种Policy语言，例如Rego、Cedar或内部DSL；
- Policy规则；
- 主体、动作、资源和数据类型定义；
- Agent类型、数据等级、处理用途等业务词典；
- 必须允许、必须拒绝、必须脱敏或审批的业务控制要求。

**处理：**

1. 将规则转换为统一的规则对象；
2. 建立规则之间的包含、重叠、覆盖、冲突和依赖关系；
3. 检查是否存在同时命中多条矛盾规则的请求；
4. 检查是否存在永远不会生效的规则；
5. 检查是否存在业务要求但没有任何规则覆盖的场景；
6. 生成一组可以实际触发问题的输入样例。

其中：

- AST是解析规则时使用的内部结构，不是另一种Policy语言；
- SAT/SMT是判断条件能否同时成立的计算方法；
- Z3是可以执行这种计算的现成工具。

**输出：**

- 缺陷类型；
- 涉及的规则编号；
- 具体触发条件；
- 风险说明；
- 修复建议；
- 可直接转成自动化测试的回归用例。

它解决的问题是：

> Policy本身有没有冲突、遗漏、重复或失效？

------

## 2. 基于因果执行轨迹重放的 Policy 反事实推演算法

这是一个“运行后的动态验证算法”。

它基于一次真实发生过的Agent任务，保持大部分条件不变，只修改指定条件，然后重新执行并比较结果。

**输入：**

- 一次完整的Agent执行轨迹；
- 用户、Agent及委托链；
- 请求的动作、资源、数据等级和用途；
- 当时使用的Policy版本；
- 数据、配置、模型和工具版本快照；
- 准备改变的条件，例如Policy版本或Agent类型；
- 禁止真实写入数据库、发送消息等安全边界。

**处理：**

1. 根据Trace编号还原完整执行过程；
2. 找到Policy判断前的检查点；
3. 保存原始状态；
4. 只修改指定变量，例如：

```
Policy v12 → Policy v13
内部Agent → 外部Agent
内部数据 → 机密数据
客户服务用途 → 模型训练用途
```

1. 在隔离环境中重新执行；
2. 比较事实执行和反事实执行的差异；
3. 找出导致结果变化的最小条件集合。

**输出：**

- 允许/拒绝是否发生变化；
- 脱敏、审批等附加要求是否变化；
- 哪些数据字段新增暴露或不再暴露；
- 哪条规则导致变化；
- 哪个最小条件变化造成结果翻转；
- 可审计的事实与反事实证据；
- 回归测试用例。

它解决的问题是：

> 如果换一个Policy版本、Agent身份或数据条件，实际决策和数据流会不会改变？

这里的“因果”指受控修改条件后重新执行，不是仅凭历史日志相关性推断因果。

------

## 两个算法的关系

```
规则图检测
→ 发现疑似Policy缺陷
→ 生成触发样例
→ 查找或构造执行Trace
→ 反事实重放
→ 验证决策和数据暴露影响
→ 生成修复建议和回归测试
```

一句话总结：

> 第一个算法负责“发现规则可能有问题”，第二个算法负责“验证这个问题在真实执行中会造成什么影响”。

Phase I建议重点实现：

- 算法一：冲突、冗余、不可达、覆盖缺口；
- 算法二：Policy版本变化、Agent身份变化、数据等级变化和用途变化；
- 两者共同输出可复现证据和自动化回归测试。